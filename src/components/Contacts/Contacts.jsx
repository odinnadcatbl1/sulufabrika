const Contacts = () => {
    return (
        <section className="contacts" id="contacts">
            <div className="container">
                <div className="contacts__inner">hello</div>
            </div>
        </section>
    );
};

export default Contacts;
