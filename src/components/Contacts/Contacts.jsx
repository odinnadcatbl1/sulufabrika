const Contacts = () => {
    return (
        <section className="contacts" id="contacts">
            <div className="container">
                <h2 className="page__title">KONTAKTI</h2>

                <div className="contacts__inner"></div>
            </div>
        </section>
    );
};

export default Contacts;
