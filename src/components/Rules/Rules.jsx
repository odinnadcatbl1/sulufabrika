const Rules = () => {
    return (
        <section className="rules" id="rules">
            <div className="container">
                <h2 className="page__title">NOTEIKUMI</h2>
                <div className="rules__inner"></div>
            </div>
        </section>
    );
};

export default Rules;
